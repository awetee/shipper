<nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><a href="#">Shipper</a></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#"></a>Menu</li>
      </ul>

      <section class="top-bar-section">
        <ul class="right">
          <li class="active"><%= link_to 'Home', { :controller=>"guests",:action => 'index'} %></li>
          <li class="has-dropdown"><%= link_to 'Browse', { :controller=>"products",:action => 'index'} %>
          </li>
          <li><a href="#">Contact</a></li>
            <% if user_signed_in? %>
          <!-- Logged in as <strong><%= current_user.email %></strong> -->
          <li><%= link_to 'Edit profile', edit_user_registration_path %></li>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign up", new_user_registration_path  %></li>
          <li><%= link_to "Login", new_user_session_path  %></li>
        <% end %>
        </ul>
      </section>
    </nav>